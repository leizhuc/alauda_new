# AI Skills Directory

This directory is intended to store custom AI skills, scripts, or specific prompt templates (e.g., `api_design_skill.md`, `refactoring_rules.md`). 
When asking the AI to perform a specific domain task, you can instruct it to first read the relevant skill file here to align with your desired "vibe" and conventions.
